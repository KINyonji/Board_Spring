/**
 * 
 */

$(document).ready(function() {
	console.log("emfjdha")
	alert('hi');
	
	$("#boardList").DataTable({ });

	/*$("#boardList").DataTable({
		dom: 'lfBtp',
		buttons: [
			{
				text: '새 글 쓰기',
				action: function(e, dt, node, config) {
					alert('Button activated');
				}
			}
		]
	});*/
});